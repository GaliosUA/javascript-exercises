const fibonacci = function(number) {
    if (number < 0) return 'OOPS';
    const phi = 1.618033988;
    return fibo = Math.round(Math.pow(phi, number) / Math.sqrt(5));
};

// Do not edit below this line
module.exports = fibonacci;
